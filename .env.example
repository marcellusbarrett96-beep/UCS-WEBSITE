# Copy this file to .env.local for local testing (never commit the real .env.local)
# In production (Vercel), set this in Project Settings → Environment Variables instead.
ANTHROPIC_API_KEY=your-key-here
